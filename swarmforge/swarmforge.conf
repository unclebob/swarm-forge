# Format: window <role> <agent> <worktree> [task|batch]
# The optional receive mode defaults to task. Adversaries currently use task
# mode only.
window coder codex master
window reviewer codex reviewer
