# Idea G — Per-Technology Engineering File

**Status:** Rejected

Adding a language to the engineering tool table costs 2-3 lines. The template system (setup prompts, generated files, install flow) is not justified for that maintenance burden. Upstream strategy is kept: add a row to the shared table when a new language is needed.
