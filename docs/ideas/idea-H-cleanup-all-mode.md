# Idea H — swarm-cleanup --all Mode

**Status:** Rejected

The scenarios it solves (orphaned sessions, multiple simultaneous swarms) are handled by a one-liner the operator can run manually, or via the cmux UI (delete the workspace group). Not worth a code change.
